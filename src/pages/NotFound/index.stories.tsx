import React from "react";
import { NotFound } from "./index";

export default {
  title: "Pages/NotFound",
  component: NotFound,
  argTypes: {},
};

export const Default: React.FC = () => {
  return <NotFound />;
};
